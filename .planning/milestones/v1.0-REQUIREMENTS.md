# Requirements Archive — v1.0

**Status:** ✅ All requirements satisfied as of 2026-06-27
**Phases covered:** 1–4

---

## Phase 1: Foundation (Pre-GSD, validated)

- [x] Auth system: register, login, JWT refresh/logout
- [x] User profile CRUD (full_name, bio, location, etc.)
- [x] Job preferences CRUD (desired_roles, locations, salary_min, etc.)
- [x] Resume upload and storage (PDF, S3-compatible, parsed_data JSON)

**Outcome:** 27 tests, 95% coverage. Validated.

---

## Phase 2: Job Discovery (Pre-GSD, validated)

- [x] Portal registry + base adapter + 4 portal adapters (Naukri, LinkedIn, Indeed, Wellfound)
- [x] Job model + Alembic migration + SHA-256 hash upsert deduplication
- [x] Celery scrape task + POST /workers/scrape endpoint
- [x] GET /api/v1/jobs/ and GET /api/v1/jobs/{id} endpoints

**Outcome:** 44 tests, 86% coverage. Validated.

---

## Phase 3: AI Matching Engine

- [x] REQ-01: Generate embeddings for jobs and user profiles using OpenAI text-embedding-3-small
- [x] REQ-02: JobMatch model storing user_id, job_id, score, ats_score, skill_gaps, status
- [x] REQ-03: pgvector cosine similarity search matching profiles to jobs
- [x] REQ-04: Match scoring Celery worker that runs asynchronously
- [x] REQ-05: GET /api/v1/jobs/{id}/match endpoint returning match score for a job
- [x] REQ-06: GET /api/v1/matches/ endpoint listing ranked matches for current user

**Outcome:** 59 tests, 87% coverage. All 6 requirements satisfied.

---

## Phase 4: Application Automation

- [x] REQ-07: Application model (user_id, job_id, status, tailored_resume_text, applied_at)
- [x] REQ-08: LLM-powered resume tailoring per job description before applying (claude-haiku-4-5)
- [x] REQ-09: One-click apply endpoint (POST /api/v1/applications/ → queues tailor_and_apply task) — *note: records + queues; actual portal submission deferred to v1.1*
- [x] REQ-10: Application status tracking (pending/tailoring/applied/rejected/interview/offer)
- [x] REQ-11: GET /api/v1/applications/ listing all user applications with status

**Outcome:** 72 tests, 87% coverage on applications/. All 5 requirements satisfied (REQ-09 partially deferred).

---

## Summary

| Phase | Requirements | Tests | Coverage |
|-------|-------------|-------|----------|
| 1 (Foundation) | 4 (pre-GSD, validated) | 27 | 95% |
| 2 (Job Discovery) | 4 (pre-GSD, validated) | 44 | 86% |
| 3 (AI Matching) | REQ-01..06 | 59 | 87% |
| 4 (App Automation) | REQ-07..11 | 72 | 87% |

_For next milestone requirements, see .planning/REQUIREMENTS.md_
